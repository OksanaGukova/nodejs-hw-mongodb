description: Not Found error
content:
  application/json:
    schema:
      type: object
      required:
        - name
        - phoneNumber
        - contactType
        - userId
      properties:
        status:
          type: integer
          example: 404
        message:
          type: string
          example: Resource not found
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'Contact not found'
